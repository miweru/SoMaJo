[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "somajo-native"
version = "0.1.0"
description = "Optional Rust acceleration for SoMaJo (grapheme/emoji segmentation)"
requires-python = ">=3.8"

[tool.maturin]
features = ["pyo3/extension-module"]
